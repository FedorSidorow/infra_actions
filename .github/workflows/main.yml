name: CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run one line script.
      run: echo Hello world Git version.
    - name: Run multi line script.
      run: 
        echo 1 line
        echo 2 line
